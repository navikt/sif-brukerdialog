import { KursperiodeMessageKeys } from '.';
import { kursperiodeMessages_nb } from './nb';

export const kursperiodeMessages_nn: Record<KursperiodeMessageKeys, string> = {
    ...kursperiodeMessages_nb,
};
