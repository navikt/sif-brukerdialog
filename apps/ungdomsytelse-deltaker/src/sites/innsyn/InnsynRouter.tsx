import { Route, Routes } from 'react-router-dom';

const InnsynRouter = () => {
    return (
        <Routes>
            <Route index element={<>sdf</>} />
        </Routes>
    );
};

export default InnsynRouter;
