sonar.projectKey=navikt_sif-brukerdialog-ungdomsytelse-deltaker
sonar.organization=navikt
sonar.projectName=Ungdomsytelse Deltaker

sonar.sources=src

# Testkode-mappen (relativ til denne filen)
#sonar.tests=src
#sonar.test.inclusions=**/*.test.ts,**/*.spec.ts,**/*.test.tsx,**/*.spec.tsx

# Filer som ikke skal analyseres
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/*.stories.tsx,**/*.test.ts,**/index.html,**/*.spec.*,**/*mock*/**

# Testdekning (eksempel med Vitest + lcov)
# Sørg for at coverage rapport genereres før SonarCloud-scan
#sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Språk
#sonar.language=ts

# Encoding
#sonar.sourceEncoding=UTF-8
