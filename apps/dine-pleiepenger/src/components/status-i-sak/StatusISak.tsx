import React from 'react';
import { Sak } from '../../types/Sak';

interface Props {
    sak: Sak;
}

const StatusISak: React.FunctionComponent<Props> = ({}) => <>Status i sak</>;

export default StatusISak;
