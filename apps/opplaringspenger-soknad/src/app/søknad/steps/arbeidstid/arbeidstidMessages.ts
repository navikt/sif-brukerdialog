const nb = {
    'steg.arbeidstid.counsellorPanel.avsnitt.1': 'Informasjon om hvor mye du er borte fra arbeid',
};

const nn: Record<keyof typeof nb, string> = { ...nb };

export const arbeidstidMessages = { nb, nn };
