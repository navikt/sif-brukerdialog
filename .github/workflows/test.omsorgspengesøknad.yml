name: test.omsorgspengesøknad

on:
    push:
        branches-ignore:
            - 'main'
        paths:
            - 'apps/omsorgspengesoknad/**/*'
            - 'nais-config/**/*'
            - 'packages/**/*'
            - .github/workflows/app.omsorgspengesøknad.yml
            - .github/workflows/test.omsorgspengesøknad.yml
    workflow_call:

env:
    APP_NAME: 'omsorgspengesoknad'

jobs:
    run-script-tests:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node.js environment
              uses: actions/setup-node@v4
              with:
                  node-version: 18
                  cache: 'yarn'

            - name: Install dependencies (yarn)
              run: yarn

            - name: Run code tests
              run: yarn test --filter=omsorgspengesoknad

    run-playwright-test:
        timeout-minutes: 60
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node.js environment
              uses: actions/setup-node@v4
              with:
                  node-version: 18
                  cache: 'yarn'

            - name: Install dependencies
              run: yarn

            - name: Install Playwright Browsers
              run: npx playwright install chromium

            - name: Build application
              run: yarn build-app --filter=omsorgspengesoknad

            - name: Run tests
              run: yarn turbo playwright-test --filter=omsorgspengesoknad
